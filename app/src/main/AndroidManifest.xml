<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.test.lpanstudyrecord"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name="com.lpan.study.context.MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name="com.lpan.study.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.lpan.study.activity.GoalActivity"/>
    </application>

</manifest>